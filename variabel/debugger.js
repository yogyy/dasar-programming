function createFullName(firstName, middleName, lastName){
    // debugger
    const fullname = `${firstName} ${lastName} ${lastName}`
    return fullname
}

const name = createFullName("Yogy", "F-S", "Constantine")
console.log(name)


// semi bold text css?